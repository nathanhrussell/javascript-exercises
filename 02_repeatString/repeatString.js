const repeatString = function(string, num) {
    let result = "";
    for (let i = 0; i < num; i++) {
        result += string
    }
    
    return result
};

// TODO: FIND A WAY TO RUN THE CODE SO AS TO TEST WHAT HAPPENS WHEN YOU USE A NEGATIVE NUMBER

// Do not edit below this line
module.exports = repeatString;
